{
  "name": "@cloudpackage/worker",
  "version": "1.0.0",
  "type": "module",
  "author": "chunkburst <chunkburst@outlook.com>",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute cloudpackage-db --local --file=./src/db/migrations/0001_initial.sql",
    "db:migrate": "wrangler d1 execute cloudpackage-db --file=./src/db/migrations/0001_initial.sql",
    "clean": "rm -rf dist .wrangler"
  },
  "dependencies": {
    "@cloudpackage/shared": "workspace:*",
    "@hono/zod-validator": "^0.8.0",
    "hono": "^4.5.0",
    "jose": "^5.6.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.90.0"
  }
}
